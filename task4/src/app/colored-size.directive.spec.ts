import { ColoredSizeDirective } from './colored-size.directive';

describe('ColoredSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColoredSizeDirective();
    expect(directive).toBeTruthy();
  });
});
